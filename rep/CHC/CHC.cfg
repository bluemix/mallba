		// number of independent runs
		// number of generations
		// number of individuals
		// display state ?
Selection-Parameters	// selections to apply
		// selection parameter , diverge operator & its probability
Intra-Operators	// operators to apply in the population
		// crossover & its probability
Inter-Operators  // operators to apply between this population and anothers
		// operator number, operator rate, number of individuals, selection of indidivual to send and remplace
LAN-configuration
		// refresh global state
		// 0: running in asynchronized mode / 1: running in synchronized mode
		// interval of generations to check solutions from other populations

